{
    "name": "image-to-ascii",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "vite --host",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "vite": "^2.9.1"
    },
    "dependencies": {
        "typescript": "^4.6.3"
    }
}
